description: run moongitd in the foreground against ./data/ (dev mode)

steps:
  - name: data/repos directory
    file: { path: data/repos, state: dir }

  - name: run moongitd
    cmd: [go, run, ./cmd/codefortd]
    changed_when: "false"
