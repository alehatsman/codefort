description: run codefortd in the foreground against ./data/ (dev mode)

steps:
  - name: data/repos directory
    file: { path: data/repos, state: dir }

  - name: run codefortd
    cmd: [go, run, ./cmd/codefortd]
    changed_when: "false"
