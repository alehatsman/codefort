# Mooncake — local state and plan artifacts
.mooncake/
*.plan.json
*.plan.yaml

# Go build artifacts
/bin/
*.test
*.out

# Runtime data (SQLite db, bare repos)
/data/

# Agent worktrees + local Claude state (also keeps dex from indexing stale
# worktree copies of the repo).
.claude/
