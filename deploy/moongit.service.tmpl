[Unit]
Description=moongit — private self-hosted git host
After=network.target

[Service]
Type=simple
ExecStart={{ binary_path }} serve
Restart=on-failure
RestartSec=3
Environment=MOONGIT_ADDR={{ addr }}
Environment=MOONGIT_DATA_DIR={{ data_dir }}

[Install]
WantedBy=default.target
